# dependencies
node_modules/

# build output
dist/

# local environment (copy .env.example to .env)
.env
.env.local

# database backups (contain password hashes — never commit)
database/backups/

# scratch artifacts / local archives (never commit)
_*.zip
_*.js
*.tmp
*.log

# editor / OS noise
.vscode/
.idea/
Thumbs.db
desktop.ini
